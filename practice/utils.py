import os

print(os.system("df -h")) # Linux df -h command to check disk space

print(os.system('uptime'))   # uptime and load average

print(os.system('free -h'))  # free -h command to check memory usage